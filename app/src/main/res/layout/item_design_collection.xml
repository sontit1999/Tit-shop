<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:bind="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="product"
            type="com.example.titshop.model.Product" />
    </data>
    <androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:card_view="http://schemas.android.com/apk/res-auto"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        card_view:cardUseCompatPadding="true"
        card_view:cardCornerRadius="7dp"
        card_view:cardElevation="@dimen/margin_5dp_">
        <RelativeLayout
            android:layout_width="wrap_content"
            android:layout_height="@dimen/heightitemCollection">
            <ImageView
                android:id="@+id/ivProduct"
                android:src="@mipmap/bg"
                bind:imageUrl="@{product.imageUrl}"
                android:scaleType="centerCrop"
                android:layout_width="@dimen/size_item_collection"
                android:layout_height="@dimen/size_item_collection"></ImageView>
            <TextView
                android:id="@+id/tvName"
                android:text="@{product.name}"
                android:padding="@dimen/margin_5dp_"
                android:layout_marginTop="@dimen/margin_5dp_"
                android:textColor="@color/colorBlack"
                android:textSize="@dimen/textview_content"
                android:layout_below="@id/ivProduct"
                android:maxWidth="@dimen/size_item_collection"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>
            <TextView
                android:id="@+id/tvPrice"
                android:text="@{product.price}"
                android:textColor="@color/colorGrey"
                android:padding="7dp"
                android:textSize="@dimen/textview_small"
                android:layout_below="@id/tvName"
                android:maxWidth="@dimen/size_item_collection"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>
        </RelativeLayout>
    </androidx.cardview.widget.CardView>
</layout>